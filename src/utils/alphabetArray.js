const arrLength = Array.from(Array(26)).map((e, i) => i + 97);
export const alphabetArray = arrLength.map((x) => String.fromCharCode(x));
//created an array from english alphabet Unicode character code
